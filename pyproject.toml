[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ceras"
version = "0.1.0"
description = "Cognitive Efficiency + Reasoning Alignment System"

[tool.setuptools.packages.find]
where = ["src"]